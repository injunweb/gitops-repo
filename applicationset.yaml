apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: microservices
  namespace: argocd
spec:
  goTepmlate: true
  goTemplateOptions: ["missingkey=error"]
  generators:
    - git:
        repoURL: https://github.com/injunweb/projects-gitops.git
        revision: HEAD
        files:
          - path: "projects/*.yaml"
  template:
    metadata:
      name: "{{base .path.filename | trimSuffix ".yaml"}}"
      namespace: argocd
    spec:
      project: "default"
      source:
        repoURL: https://github.com/injunweb/projects-gitops.git
        targetRevision: HEAD
        path: charts
        helm:
          valueFiles:
            - "{{path.path}}"
          values: |
            project: "{{base .path.filename | trimSuffix ".yaml"}}"
      destination:
        server: https://kubernetes.default.svc
        namespace: "{{base .path.filename | trimSuffix ".yaml"}}"
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
